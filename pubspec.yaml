name: cupertino_date_time_picker_loki
description: 日期时间选择器 footer widget added by LOKI
version: 0.0.4
homepage: https://github.com/weahforsage/cupertino_date_time_picker_loki

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
